# RecoveryAI
RecoverAI is an AI agent that detects at-risk failed payments, predicts recovery probability via XGBoost, and applies a policy engine to retry, remind, wait, or escalate — recovering 43.5% of revenue vs 40.8% for blind retry, with full audit logging and safety limits.
